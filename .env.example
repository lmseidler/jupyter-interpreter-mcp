# Jupyter Server Configuration
# URL of the remote Jupyter server (container or cloud instance)
# Default: http://localhost:8888
JUPYTER_BASE_URL=http://localhost:8889

# Authentication
# Get token from container logs: docker logs <container-name> 2>&1 | grep token=
JUPYTER_TOKEN=your-token-here

# Sessions Storage Path (inside container/remote environment)
# Default for jupyter/minimal-notebook is /home/jovyan
# This path must exist in the remote environment
# All session directories will be created here
SESSIONS_DIR=/home/jovyan/sessions

# Session Time-To-Live (in seconds)
# Sessions inactive for longer than this will be cleaned up
# 0 = no expiry (sessions persist indefinitely)
# Default: 0
SESSION_TTL=0

# Restore sessions on startup
# false = faster startup, sessions restore on first execute_code call
# true = eagerly restore all sessions during startup
RESTORE_SESSIONS_ON_STARTUP=false

# ============================================================================
# Docker Setup Example
# ============================================================================
#
# 1. Start Jupyter container with port mappings:
#
#    docker run -d \
#      --name jupyter-notebook \
#      -p 8889:8888 \
#      jupyter/minimal-notebook:latest
#
# 2. Get the authentication token from container logs:
#
#    docker logs jupyter-notebook 2>&1 | grep token=
#
#    Look for a line like:
#    http://127.0.0.1:8888/lab?token=abc123def456...
#
# 3. Copy the token value (abc123def456...) to JUPYTER_TOKEN above
#
# 4. Verify Jupyter is accessible:
#
#    curl http://localhost:8889/api/kernels -H "Authorization: token abc123def456..."
#
# ============================================================================
# Troubleshooting
# ============================================================================
#
# Cannot connect to Jupyter server:
#   - Verify container is running: docker ps
#   - Check port mapping: docker port jupyter-notebook
#   - Test HTTP connection: curl http://localhost:8889/api
#
# Authentication failed:
#   - Verify token is correct: docker logs jupyter-notebook 2>&1 | grep token=
#   - Check token hasn't expired (restart container to get new token)
#
# Code execution timeout:
#   - Check kernel is running: docker exec jupyter-notebook jupyter kernel list
#   - Verify WebSocket connection is not blocked by firewall
#
